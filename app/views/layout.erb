<!DOCTYPE html>
<html lang="en">
<head>
  <!--
    normalize.css removes cross-browser differences in defaults, e.g.,
    differences in how form elements appear between Firefox and IE
    See: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/application.js"></script>

  <title></title>
</head>
<body>
  <nav>
    <ul>
      <% if current_user %>
        <a href="/">Home</a>
        <a href="/users/<%= current_user.id %>">Profile</a>
        <a href="/sessions/<%= current_user.id %>">Sign Out</a>
      <% else %>
        <form name="user" action="/sessions/error" method="post">
          E-mail: <input type="text" name="user[email">
          Password: <input type="password" name="user[password]">
          <input type="submit" name="signin" value="Sign In">
          <input type="submit" name="signin" value="Sign Up">
        </form>
        <p class="error"><%= @error %></p>
      <% end %>
    </ul>
  </nav>

  <%= yield %>
</body>
</html>
